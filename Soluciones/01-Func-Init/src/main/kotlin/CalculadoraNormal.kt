package dev.joseluisgs

class CalculadoraNormal {
    fun sumar(a: Int, b: Int) = a + b
    fun restar(a: Int, b: Int) = a - b
    fun multiplicar(a: Int, b: Int) = a * b
    fun dividir(a: Int, b: Int) = a / b
}